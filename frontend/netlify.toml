[build]
  command = "cd .. && npm install --workspace=shared --workspace=frontend && npm run build --workspace=shared && cd frontend && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
